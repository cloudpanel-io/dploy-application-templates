project:
  git_repository: '{git_repository}'
  deploy:
    directory: '{deploy_directory}'
    shared_directories:
      - 'var/log'
    before_commands:
      #- 'cd {release_directory} && touch test-file'
    after_commands:
      #- 'cd {release_directory} && touch test-file'
      #- 'bash clpctl varnish-cache:purge --purge=all' # PURGE Varnish Cache
      - 'sudo systemctl reload php8.2-fpm'